# .readthedocs.yml
# Read the Docs configuration file
version: 2

# Python config
build:
  image: latest

python:
  version: 3.7
  install:
    - requirements: requirements.txt
    - requirements: requirements_dev.txt
    - method: pip
      path: ./skrf

