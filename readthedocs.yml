# .readthedocs.yml
# Read the Docs configuration file
version: 2

# Python config
build:
  image: latest

python:
  version: "3.8"
  install:
    - requirements: requirements.txt
    - requirements: requirements_dev.txt
    - method: pip
      path: .

