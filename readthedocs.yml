# .readthedocs.yml
# Read the Docs configuration file
version: 2

# Python config
build:
  image: latest

python:
  version: 3.7
  pip_install: true
  install:
    - requirements: requirements.txt
    - requirements: requirements_dev.txt

